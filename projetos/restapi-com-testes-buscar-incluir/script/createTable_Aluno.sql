-- Criação da tabela ALUNO baseada na model Java
CREATE TABLE ALUNO (
    ID     NUMBER(19) GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NOME   VARCHAR2(150) NOT NULL,
    CURSO  VARCHAR2(150) NOT NULL
);